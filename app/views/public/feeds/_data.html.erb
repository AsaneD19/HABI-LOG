<div class="card mb-3">
<% if feed.current_duration == 0 %>
  <div class="card-header text-white bg-primary h3">
    NEW HABIT : <%= feed.habit.name %>
  </div>
<% else %>
  <div class="card-header text-white bg-success h3">
    RECORD : <%= feed.habit.name %>
  </div>
<% end %>
  <div class="card-body bg-light">
    <h4 class="card-title d-flex text-dark">
      <div class="card-title"><%= feed.member.name %></div>
      <div class="card-title text-muted">(<%= feed.member.account_id %>)</div>
    </h4>
    <p class="card-text text-dark">
      <%= feed.text_content %>
    </p>
    <h6 class="text-muted text-right">
      <%= link_to tag_path(feed.habit.tag) do %>
        #<%= feed.habit.tag.name %>
      <% end %>
    </h6>
    <h6 class="text-muted text-right"><%= l feed.created_at %></h6>
    <div class="d-flex">
      <p><%= render 'public/favorites/form', favorable: feed %>　</p>
      <p><i class="fa-regular fa-message"></i><%= feed.post_comments.count %></p>
    </div>
  </div>
</div>