<%= link_to "戻る", member_path(@member) %>

<h1>フォロー中一覧</h1>

<% if @members.count == 0 %>
  <font color="red">フォローしていません</font>
<% else %>
  <%= render "public/members/list", members: @members %>
<% end %>